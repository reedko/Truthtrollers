# Use Python 3.10-3.12. PyTorch wheels may not yet support the repository host's Python 3.14.
torch>=2.4,<3
transformers>=4.56,<6
datasets>=3,<5
trl>=0.24,<1
peft>=0.17,<1
accelerate>=1,<3
huggingface-hub>=0.27,<2
safetensors>=0.4,<1
# Required only for qlora_4bit=true on a supported CUDA environment.
bitsandbytes>=0.45,<1; platform_system == "Linux"
